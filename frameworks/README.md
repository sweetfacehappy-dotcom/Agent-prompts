# Frameworks

Framework/context documents explain where an agent fits, why it exists, what inputs and outputs it expects, what assumptions it makes, and how to understand or maintain it later.

Each framework should link to one or more separate prompt documents under `../prompts/`.
