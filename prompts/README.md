# Prompts

Every prompt must be stored as a separate Markdown document.

Use subdirectories per agent, for example:

```text
prompts/<agent-id>/system-prompt.md
prompts/<agent-id>/<additional-prompt>.md
```

Prompt documents should link back to their framework/context document under `../frameworks/`.
